# 文字入りアイコンメーカー デザインレビュー

全体として、機能に必要な要素は揃っており、白を基調としたシンプルな画面にまとめられています。ここからさらに使いやすくするために、ユーザーがサービス内容を理解し、画像を編集してダウンロードするまでの流れを中心に見直していきましょう。

以下では、良かった点と改善したい点を、対象ページ・対象箇所ごとにまとめています。一度にすべてを直すのではなく、まずは「最優先で取り組みたい項目」から進めてください。

## レビュー概要

### 対象

文字入りアイコンメーカーの以下の画面を、デスクトップ表示とモバイル表示（390 × 844px）で確認した。

- ウェルカムページ（`/welcome`）
- アイコン合成ページ（`/icons/new`）
- 利用規約ページ（`/terms`）
- プライバシーポリシーページ（`/privacy`）

### 総評

白とグレーを中心に要素が整理され、全体として清潔感のあるデザインになっている。一方、トップページではサービス内容の伝え方、アイコン合成ページでは操作の流れと描画結果の安定性に改善の余地がある。特に、最終目的である画像のダウンロードまで迷わず進める構成を意識したい。

### 最優先で取り組みたい項目

1. 描画の不具合を修正する（`DR-018`、`DR-023`）
2. ダウンロードを最終ゴールとして明確にする（`DR-024`）
3. 任意の文字入力と文字サイズ変更に対応する（`DR-016`）
4. 背景の有無をチェックボックスで切り替えられるようにする（`DR-017`）
5. トップページでサービス内容が一読で伝わるようにする（`DR-006`、`DR-026`）
6. 未ログイン時の不要な導線を整理する（`DR-020`、`DR-022`）

---

## 改善したい点

### DR-001 アップロード前でも編集操作が有効に見える

#### 対象ページ

アイコン合成ページ（`/icons/new`）

#### 対象箇所

「元画像」「プレビュー」の右または下に表示される「合成オプション」全体。

具体的には以下の操作。

- 「ラジオ参加」「離席中」「画面見れません」ボタン
- 「文字の背景をつける」ボタン
- 文字色、背景色のカラーピッカー
- 透明度スライダー

#### 詳細

画像をアップロードしていない初期状態でも、合成オプションが通常の操作可能な見た目で表示されている。一方、「画像をダウンロード」だけが無効表示になっているため、ユーザーはアップロード前に編集操作ができるのか判断しづらい。

画面上部には「まずはアイコンをアップロードしましょう」と書かれているが、操作領域全体の見た目からは手順の依存関係が十分に伝わらない。

#### 改善案

画像が選択されるまでは、合成オプション全体を無効化し、薄い表示にする。操作できない理由として「画像を選択すると編集できます」のような補足を表示する。

あわせて、作業の順序を次のように明示すると理解しやすい。

1. 画像を選ぶ
2. 文字や背景を設定する
3. 画像をダウンロードする

#### 関連実装

`app/views/icons/new.html.slim` のアイコンアップロード案内と合成オプション部分。

---

### DR-002 外部サービスの変更ガイドが主作業と一体化し、セクションの範囲がわかりにくい

#### 対象ページ

アイコン合成ページ（`/icons/new`）

#### 対象箇所

編集ツールの下にある「オンラインイベントサービスなどの各リンクと変更ガイド」セクションと、それを内包している白いコンテンツ領域。

対象となるサービスカードは以下。

- Remo
- Zoom
- Slack
- Discord
- Google

#### 詳細

アイコンの合成がページの主目的だが、外部サービスの変更ガイドが常に展開され、編集ツールと同程度の視覚的な強さで表示されている。

特にモバイルでは5枚のカードが縦に並び、ページ全体の大部分を占める。編集やダウンロードを目的としているユーザーにとっては、補助情報の量が多く、主作業への集中を妨げる可能性がある。

また、すべてのカードが似た見た目であるため、目的のサービスを視覚的に探しにくい。

ガイドセクション全体が白いコンテンツ領域に入り、その中で各サービスも枠線付きカードとして表示されているため、「カードの中にカード」が並ぶ入れ子構造に見える。枠線、角丸、内側の余白が重複し、視覚的にしつこいうえ、各サービスの情報量に対して縦方向のスペースを取りすぎている。

各サービス名も小さなリンク文字で表示されているため、一覧から利用中のサービスを探すための主要ラベルとして目立っていない。手順本文との文字サイズの差が小さく、カードや項目の見出しとしての階層も弱い。

さらに、アイコン合成ツールと変更ガイドが同じ大きな白いカードの中に連続して配置されているため、「オンラインイベントサービスなどの各リンクと変更ガイド」という見出しがどこからどこまでを指しているのかわかりにくい。見出しの上に区切り線はあるものの、編集機能から独立した別セクションであることが視覚的に十分伝わっていない。

見出しの文言も「オンラインイベントサービスなど」「各リンク」「変更ガイド」と複数の情報を並べた提供者側の説明になっており、長くて要点を把握しづらい。ユーザーがこのセクションで何をできるのかを、より短く直接伝える必要がある。

#### 改善案

アイコン合成ツールと変更ガイドを、それぞれ独立したカードまたはコンテンツ領域に分ける。2つの領域の間には背景が見えるだけの十分な余白を設け、見出しが支配する範囲を境界で明確にする。

ただし、変更ガイドの外側をカードとして表現する場合、各サービスをさらに個別のカードで囲まない。サービス一覧は、シンプルなリスト、区切り線付きの行、またはフラットなアコーディオンとして表現する。

各サービス行は、例えば以下の最小限の要素で構成する。

- サービス名またはロゴ
- 「変更手順を見る」という操作
- 展開状態を示すアイコン

枠線と角丸を何重にも使わず、余白と細い区切り線によって項目を判別できるようにする。

サービス名は少なくとも本文の基準サイズである `1rem`（16px）以上にし、必要に応じて太字にする。手順本文より明確に強く見せ、サービスのロゴと横並びにすることで、一覧を流し見しても目的の項目を見つけられるようにする。

見出しは、ユーザーが次に行う操作を表す以下の文言へ変更する。

> 各サービスでアイコンを変更する

「リンク」や「ガイド」といったコンテンツの形式ではなく、ユーザーが達成したい目的を見出しにする。必要であれば、見出しの下に短い補足を置く。

> 利用しているサービスを選ぶと、アイコンの変更手順を確認できます。

そのうえで、サービスごとの手順をアコーディオン形式にする。初期状態ではサービス名だけを表示し、必要な項目だけ展開できるようにする。

別案として、ダウンロードが完了した後に「各サービスでアイコンを変更する」という次の行動として案内する方法も考えられる。

各サービスの公式ロゴまたは識別用アイコンを追加すると、目的のサービスを探しやすくなる。

#### 関連実装

`app/views/icons/new.html.slim` のメインコンテンツを囲む白い領域と、`app/views/icons/_icon_change_links.html.slim` のガイドセクション。ガイドを独立したカードとして配置できるよう、現在の包含関係を見直す。

---

### DR-003 モバイルのタップ領域が小さい

#### 対象ページ

アイコン合成ページ（`/icons/new`）を中心としたモバイル表示。

#### 対象箇所

- ヘッダーの「ログイン」リンク
- 「← アイコン一覧へ」リンク
- テキスト追加ボタン
- フッターの各リンク
- GitHubリンク

#### 詳細

390px幅で確認したところ、主な操作要素の高さは以下だった。

- テキスト追加ボタン：約34px
- 「← アイコン一覧へ」リンク：約30px
- 「ログイン」リンク：約20px
- フッターリンク：約20px

文字そのものは読めるが、指で操作するにはタップ可能領域が狭い。リンク同士が近いフッターでは、押し間違いも起こりやすい。

なお、「ログイン」と「← アイコン一覧へ」は別項目で非表示または削除を提案している。これらを残す場合は、ほかの操作要素と同様に十分なタップ領域を確保する。

#### 改善案

モバイルでは、主要なボタンとナビゲーションリンクのタップ領域を最低44 × 44px程度確保する。文字サイズを大きくしすぎる必要はなく、上下のパディングやリンク周囲の余白を増やして対応できる。

#### 関連実装

`app/assets/tailwind/application.css` の `.btn-text-option`、`.link-button` などのコンポーネントスタイル、およびヘッダー・フッターのリンクスタイル。

---

### DR-004 初期状態のプレビュー領域が空白で、完成イメージを想像しづらい

#### 対象ページ

アイコン合成ページ（`/icons/new`）

#### 対象箇所

完成結果を表示する「プレビュー」の正方形フレーム。

#### 詳細

画像をアップロードする前は薄いグレーの枠だけが表示される。何が表示され、どのような完成物が得られるのかを、初めて利用するユーザーが画面だけで想像するのは難しい。

#### 改善案

「元画像」の専用プレビューは `DR-019` のとおり削除し、完成プレビュー内に以下のような初期表示を追加する。

- 「文字入り画像の完成イメージ」
- 画像アイコンなどのプレースホルダー

または、薄く表示したサンプル画像を使い、アップロード後に実画像へ置き換える。

#### 関連実装

`app/views/icons/new.html.slim` の「プレビュー」フレーム部分。

---

### DR-005 カラーと透明度の現在値がわかりにくい

#### 対象ページ

アイコン合成ページ（`/icons/new`）

#### 対象箇所

「合成オプション」内の以下のコントロール。

- 文字色
- 背景色
- 透明度

#### 詳細

文字色と背景色は小さな色の矩形だけで示され、色コードが画面上に表示されない。透明度スライダーも現在値が数値で表示されないため、設定結果を正確に把握したり、同じ設定を再現したりしづらい。

#### 改善案

各設定に現在値を併記する。

- `文字色 #000000`
- `背景色 #000000`
- `透明度 100%`

色コードを直接入力できるフィールドを追加すると、細かな調整や設定の再現もしやすくなる。

#### 関連実装

`app/views/icons/new.html.slim` のカラー設定フィールドと透明度スライダー部分。

---

### DR-006 ウェルカムページに完成例がなく、説明文の中央揃えも読みにくい

#### 対象ページ

ウェルカムページ（`/welcome`）

#### 対象箇所

メイン見出しの下にある以下のサービス説明と、ファーストビュー全体の構成。

> 自分のアイコン画像に「今日はラジオ参加」などのテキストを1クリックで合成し、ダウンロードすることができます。  
> 主要なサービスのアイコン変更リンク、ガイドが揃っているのですぐにアイコンを変更することができます。

#### 詳細

サービスの説明文と主要なボタンは明快だが、実際にどのような画像を作れるのかを示す視覚的な例がない。

特にデスクトップでは画面下部に大きな余白があり、文章を読まなければサービスの価値が伝わりにくい。アイコン加工という視覚的なサービスなので、完成例があると理解が速くなる。

また、比較的長い説明文が中央揃えになっているため、表示幅によって不自然な位置で改行される。各行の開始位置も揃わず、次の行を目で追いにくい。短いキャッチコピーには中央揃えが適しているが、複数行にわたる説明文は左揃えの方が読みやすい。

#### 改善案

ファーストビューを「ビジュアル」と「説明文」の2カラム構成にする。

ビジュアル側には、加工前と加工後を比較できるサンプルを配置する。

- 通常のアイコン
- 「ラジオ参加」などの文字を合成したアイコン
- 変化を示す矢印

文章側には、見出し、サービス説明、主要なボタンをまとめる。説明文は左揃えにし、一行が長くなりすぎない幅に制限する。

デスクトップではビジュアルと文章を左右に並べ、モバイルでは1カラムへ切り替える。モバイルでは、サービス内容を先に理解させたい場合はビジュアルを文章の前に、操作を優先したい場合は見出しと文章の後に配置する。

#### 関連実装

`app/views/home/welcome.html.slim` のメインコンテンツ部分。現在コンテナ全体へ指定されている中央揃えを見直し、見出しやボタンと、長文の説明文で配置ルールを分ける。

---

### DR-007 トップページではヘッダーをなくし、ロゴをより大きく見せたい

#### 対象ページ

ウェルカムページ（`/welcome`）

#### 対象箇所

ページ最上部のヘッダーと、ヘッダー左端に配置されているサイトロゴ。

#### 詳細

トップページでは、ヘッダー内に小さなロゴだけが配置されている。ナビゲーションとして必要な項目がほとんどないため、独立したヘッダーを設ける必要性が低い。

現在のロゴは画面左上に小さく表示されており、サービスの第一印象を作るブランド要素として十分に目立っていない。特にデスクトップでは、ロゴの周囲に余白がある一方でロゴ自体が小さいため、ヘッダーが単なる空白の帯に見えやすい。

#### 改善案

トップページでは共通ヘッダーを表示せず、ロゴをメインコンテンツの冒頭へ移動する。ロゴは現在より大きくし、見出しやサービス説明と一体になったヒーロー領域として見せる。

配置例は以下のとおり。

1. 大きめのロゴ
2. 「オンラインイベントで『ラジオ参加』を発言せずに伝える」という見出し
3. サービスの説明または完成例
4. 「アイコンに文字を合成する」ボタン

トップページ以外では、画面移動に必要な要素だけを配置した、必要最小限の共通ヘッダーを維持する。未ログイン時の「ログイン」リンクは `DR-022` のとおり削除する。

#### 関連実装

`app/views/layouts/application.html.slim` とヘッダーの部分テンプレートで、ウェルカムページのみヘッダーを非表示にする。ロゴは `app/views/home/welcome.html.slim` のメインコンテンツへ移動し、トップページ専用のサイズを指定する。

---

### DR-008 トップページの日本語見出しの行間が狭い

#### 対象ページ

ウェルカムページ（`/welcome`）

#### 対象箇所

ファーストビューにある以下のメイン見出し。

> オンラインイベントで  
> 「ラジオ参加」を発言せずに伝える

#### 詳細

2行に分かれた見出しの行間が狭く、1行目と2行目が詰まって見える。日本語は漢字、ひらがな、かぎ括弧など形の密度が高く、欧文と同じ感覚で行間を設定すると窮屈になりやすい。

この見出しでは2行目にかぎ括弧が含まれ、文字サイズと太さも大きいため、行同士の視覚的な接近が特に目立つ。トップページで最も重要なメッセージなので、読みやすさだけでなく、ゆったりとした第一印象も必要になる。

#### 改善案

見出しに明示的な行間を設定し、現在より広げる。目安として `line-height: 1.35` 前後から確認し、文字サイズや画面幅に応じて調整する。

デスクトップとモバイルでは折り返し位置が変わる可能性があるため、両方の表示で以下を確認する。

- 行同士が詰まって見えないこと
- 見出し全体が間延びして見えないこと
- かぎ括弧や漢字の上下に十分な余白があること
- 意図しない3行以上の折り返しが発生しないこと

#### 関連実装

`app/views/home/welcome.html.slim` のメイン見出しに指定されている行間クラス。現在の見出しへ、日本語の複数行表示に適した `leading-*` クラスを追加する。

---

### DR-009 「登録なしですぐに使える」という重要な特徴が十分に訴求できていない

#### 対象ページ

ウェルカムページ（`/welcome`）

#### 対象箇所

「登録なしですぐに使えます！」という見出しと、その下にある以下の説明項目。

- ログインせずにアイコンへテキストを追加し、ダウンロードできる
- ログインすると、合成したアイコンをサイトへ保存できる

#### 詳細

登録なしで主要機能を利用できることは、サービスを試す際の心理的な障壁を下げる重要な特徴である。しかし、現在は薄い背景色の一つの囲みの中に、2項目が通常の箇条書きとして表示されているため、他の説明文に近い見え方になっている。

「登録せずにできること」と「ログインすると便利になること」は性質の異なる情報だが、同じリスト内に並んでいるため、その違いも直感的に把握しづらい。

また、現在の「合成したアイコンをサイトに保存したい場合はログインが必要です」という表現は、ログインを制約や条件として感じさせる。ログインを促す場合は、必要性を説明するよりも、ログインによって得られる便利さを伝える方が前向きな印象になる。

#### 改善案

2つの内容をそれぞれ独立したカードとして表現し、各カードに内容を連想できるイメージ画像またはイラストを添える。

カードの例は以下のとおり。

1. **登録なしで作成・ダウンロード**
   - アイコンへ文字を追加しているイメージ
   - ダウンロードを示すイメージ
   - 「今すぐ使える」という短い補足
2. **ログインでもっと便利に**
   - 作成済みアイコンが一覧に保存されているイメージ
   - Googleログインを示す控えめな案内
   - 「作ったアイコンをサイトに保存できます」のような、得られる価値を示す説明

登録なしで利用できるカードを視覚的に強くし、ログインのカードは補助的な見せ方にする。これにより、ログインが利用開始の必須条件であると誤解されるのを防げる。

ログイン側の文章は、「ログインが必要です」のような制約を中心にせず、次のような前向きな表現にする。

> Googleアカウントでログインすると、作ったアイコンをサイトに保存できます。

または、より短くカード内で見せる場合は次のようにする。

> ログインでもっと便利に  
> 作ったアイコンをサイトに保存できます。

デスクトップでは2枚のカードを横並びにし、モバイルでは縦に並べる。画像は装飾だけにせず、各項目の違いを一目で理解できる内容にする。

#### 関連実装

`app/views/home/welcome.html.slim` の「登録なしですぐに使えます！」を表示している `blockquote` 部分。箇条書きをカード構成へ変更し、カード用の画像またはイラストを配置する。

---

### DR-010 2つのCTAが異なる目的の選択肢に見える

#### 対象ページ

ウェルカムページ（`/welcome`）

#### 対象箇所

ファーストビュー下部に縦並びで表示されている以下の2つのCTA。

- 「アイコンに文字を合成する」
- 「Googleアカウントでログイン」

#### 詳細

現在のラベルでは、一方が「アイコンを作る」という目的、もう一方が「ログインする」という手段を表している。そのため、異なる種類の操作が並んでいるように見え、ユーザーはどちらを選べばアイコン作成を始められるのか判断しづらい。

実際には、どちらも「アイコンに文字を合成する」という同じ目的への入口であり、選択の違いはログインするかどうかにある。この関係をボタンの文言だけで理解できるようにする必要がある。

#### 改善案

2つのCTAを、同じ目的に対するログイン有無の選択肢として表現する。

- 「ログインしてアイコンに文字を合成する」
- 「ログインせずにアイコンに文字を合成する」

より短く見せる場合は、共通の見出しとボタンラベルを分ける方法もある。

**案内見出し**

> どちらの方法で始めますか？

**ボタン**

- 「ログインして作る」
- 「ログインせずに作る」

ログインによる保存機能も同時に伝える場合は、ボタンの近くに短い補足を添える。

- 「ログインして作る」— 作ったアイコンをサイトに保存できます
- 「ログインせずに作る」— 登録なしですぐに試せます

この補足はボタンのラベルへすべて詰め込まず、ボタンの下または選択カード内に分けて表示する。

#### 関連実装

`app/views/home/welcome.html.slim` の「アイコンに文字を合成する」リンクと「Googleアカウントでログイン」ボタンを表示しているCTAセクション。

---

### DR-011 コンテンツが少ないページでもフッターを見るためにスクロールが必要

#### 対象ページ

ウェルカムページ（`/welcome`）を含む、コンテンツ量が画面の高さより少ないページ。

#### 対象箇所

ページ下部のフッターと、ヘッダー・メインコンテンツ・フッターを含む共通レイアウト全体。

フッターには以下のリンクがある。

- 利用規約
- プライバシーポリシー
- GitHub

#### 詳細

コンテンツ量が少ないページでも、フッターのリンクを見るためにわずかなスクロールが必要になる場合がある。一方、フッターを単純にメインコンテンツの直後へ配置すると、画面の途中に浮いて見え、ページ下部に不自然な空白が残る可能性がある。

短いページではフッター全体が初期表示内に収まり、かつ画面の下端に配置されることが望ましい。コンテンツが長いページでは、フッターが画面へ固定されて本文を覆うのではなく、本文の後に通常どおり表示される必要がある。

#### 改善案

共通レイアウトを縦方向のFlexboxで構成する、いわゆるsticky footerを採用する。

1. ページ全体に最低でも画面と同じ高さを持たせる
2. ヘッダー、メインコンテンツ、フッターを縦方向に並べる
3. メインコンテンツを伸縮可能にし、余った高さを埋める
4. フッターをメインコンテンツの後に配置する

これにより、短いページではフッターがスクロールせずに見える画面下部へ配置され、長いページではコンテンツの末尾に続いて表示される。

フッターを `position: fixed` で固定すると、画面の小さい端末で本文や操作要素を覆う可能性があるため避ける。

#### 関連実装

`app/views/layouts/application.html.slim` のページ全体を囲む要素に、`min-height: 100vh` 相当の指定と縦方向のFlexboxを適用する。メイン要素には残りの高さを埋める指定を追加し、既存のフッター部分テンプレートはその後に配置する。

---

### DR-012 「まずはアイコンをアップロードしましょう」が見出しに見えない

#### 対象ページ

アイコン合成ページ（`/icons/new`）

#### 対象箇所

アイコン合成ツールの先頭にある「まずはアイコンをアップロードしましょう」という案内。

#### 詳細

このテキストは、ユーザーが最初に行う操作を示すセクション見出しの役割を持っている。しかし、現在は本文に近い文字サイズで表示されているため、見出しとして認識しづらい。

直下の「アイコンを選択」ボタンの方が色と面積によって強く見え、見出しとの階層が曖昧になっている。ページを流し読みした際にも、ここが作業の開始地点であることを捉えにくい。

#### 改善案

文字サイズ、太さ、下側の余白を増やし、セクション見出しとして明確に見せる。

例えば、次のような表現が考えられる。

- 文字サイズを現在より1〜2段階大きくする
- 太字を維持または少し強める
- 見出しと操作説明の間に十分な余白を設ける
- 必要であれば「STEP 1」などの短いラベルを添える

文言についても、より短く操作を直接示す「アイコンをアップロード」のような見出しと、「まずは使用する画像を選択してください」という補足文へ分けると、階層がさらに明確になる。

見た目だけでなく、HTML上でも段落ではなく適切な見出し要素として扱い、ページ内の情報構造と一致させる。

#### 関連実装

`app/views/icons/new.html.slim` の `instruction-text` が指定された案内部分と、`app/assets/tailwind/application.css` の `.instruction-text` スタイル。

---

### DR-013 「他の人が作ったプリセット」だけでは何ができるのかわからない

#### 対象ページ

アイコン合成ページ（`/icons/new`）

#### 対象箇所

プリセットが登録されている場合に表示される「他の人が作ったプリセット」という見出しと、その下のプリセット一覧。

#### 詳細

現在の見出しは、表示されているものが「他の人が作ったプリセット」であることは説明しているが、ユーザーがそのプリセットを選択して自分のアイコンへ適用できることを示していない。

初めて使うユーザーには、プリセットが単なる作例なのか、クリックして利用できる操作要素なのか判断しづらい。見出しはコンテンツの分類だけでなく、その場所でできる行動を伝える必要がある。

#### 改善案

見出しを、プリセットを利用できることが直接伝わる表現へ変更する。

推奨する文言は以下。

> 他の人が作ったプリセットを使う

より短くする場合は、次の表現も考えられる。

> プリセットから選ぶ

必要に応じて、見出しの下に操作方法を示す短い補足を追加する。

> 好きなデザインを選ぶと、文字色や背景色をアイコンに適用できます。

プリセット自体もクリック可能であることが伝わるように、ホバー・フォーカス・選択状態を明確にする。

#### 関連実装

`app/views/icons/new.html.slim` のプリセット一覧部分にある「他の人が作ったプリセット」という見出し。

---

### DR-014 サイト全体で文字が小さい

#### 対象ページ

ウェルカムページ、アイコン合成ページ、フッターを含むサイト全体。

#### 対象箇所

特に以下のテキスト。

- アイコン合成ページの操作説明
- 合成オプションのラベル
- アップロード可能なサイズとファイル形式の説明
- ダウンロード付近の補足
- 外部サービスの変更ガイド
- ヘッダーとフッターのリンク

#### 詳細

サイト全体で12〜14px程度の小さな文字が多く使われており、本文や操作に必要な説明まで補助情報のように見える。画面内へ情報を収めやすい一方、可読性が下がり、見出し・本文・注釈の階層も弱くなっている。

特にモバイルでは、画面との距離、端末の表示密度、周囲の明るさなどによって小さな文字がさらに読みづらくなる。ボタン内の文字が小さい場合は、タップ対象としての存在感も弱くなる。

デザインを検討する際は、ブラウザの標準的な本文サイズである `1rem = 16px` を基準として考える必要がある。

#### 改善案

本文および操作に必要な説明は、原則として `1rem`（16px）を基準にする。

サイズの目安は以下。

- 本文、主要な説明、フォームラベル：16px以上
- ボタン、ナビゲーション：16pxを基本とし、十分なタップ領域も確保する
- 補足、注釈、ファイル条件など：14px程度
- 12px：著作権表記など、重要度が低い短い情報に限定する

単純にすべてを同じサイズへ拡大するのではなく、本文を16pxへ引き上げたうえで、見出しをさらに大きくし、情報の階層を保つ。文字サイズの変更後は、行間、カード内の余白、ボタンの高さも合わせて調整する。

#### 関連実装

`app/assets/tailwind/application.css` と各Slimテンプレートに指定されている `text-xs`、`text-sm` を見直す。本文や主要操作へ使われている箇所は `text-base` を基準とし、`text-xs` と `text-sm` は補助情報に限定する。

---

### DR-015 アップロード条件の「となっております」が冗長

#### 対象ページ

アイコン合成ページ（`/icons/new`）

#### 対象箇所

「アイコンを選択」ボタンの横または下に表示されるアップロード条件。

現在の文言は以下。

> 合成できるアイコンのサイズは5MBまでとなっております。  
> ファイル形式は JPG, JPEG, PNG, WEBP にのみ対応しています。

#### 詳細

「5MBまでとなっております」は丁寧だが、操作画面の短い案内としては冗長に感じられる。ファイル選択前に確認する条件なので、短く直接的な表現の方が素早く把握できる。

#### 改善案

「となっております」を削除し、次のように変更する。

> 合成できるアイコンのサイズは5MBまでです。  
> 対応形式は JPG、JPEG、PNG、WEBP です。

さらに簡潔にラベル形式で見せる場合は、次の表現も考えられる。

> 最大ファイルサイズ：5MB  
> 対応形式：JPG、JPEG、PNG、WEBP

アップロード条件は文章として読ませるより、必要な値を拾いやすい簡潔な表現にする。

#### 関連実装

`app/views/icons/new.html.slim` の「アイコンを選択」ボタン付近にあるファイルサイズとファイル形式の説明。

---

### DR-016 合成する文字を自由に入力できず、文字サイズも変更できない

#### 対象ページ

アイコン合成ページ（`/icons/new`）

#### 対象箇所

「合成オプション」内の「テキストを追加」セクション。

現在は以下の定型文ボタンが表示されている。

- ラジオ参加
- 離席中
- 画面見れません

#### 詳細

ユーザーは用意された定型文から選択できるが、自分で任意の文字を入力できない。そのため、「耳だけ参加」「作業中」「カメラOFF」など、利用場面に合った表現を作れず、アイコンメーカーとしての自由度が低い。

また、文字サイズを変更する操作がないため、文字数や画像の構図に合わせた調整ができない。短い文字では小さく見えたり、長い文字では窮屈になったりしても、ユーザー側で解決できない。

#### 改善案

定型文ボタンは入力を省略できる便利な選択肢として残し、その近くに任意の文字を入力できるテキストフィールドを追加する。

構成例は以下。

1. 「表示する文字」というラベル付きテキストフィールド
2. 「ラジオ参加」などの定型文を入力できる候補ボタン
3. 「文字サイズ」というラベル付きのスライダーまたは数値入力
4. 現在の文字サイズを示す数値

定型文ボタンを押した場合は、その文字をテキストフィールドへ入力する。ユーザーは入力後に自由に編集できるようにする。

文字サイズは、例えばスライダーで直感的に変更できるようにし、現在値を `32px` のように併記する。キーボードで正確に調整できる数値入力を組み合わせてもよい。

入力内容と文字サイズの変更は、プレビューへ即時反映する。長い文章を入力した場合の最大文字数、折り返し、縮小、はみ出しの扱いも定め、ユーザーが結果を予測できるようにする。

#### 関連実装

`app/views/icons/new.html.slim` の「テキストを追加」部分と、`app/javascript/controllers/canvas_controller.js` の文字描画処理。任意テキストと文字サイズを描画処理へ渡せるようにする。

---

### DR-017 文字背景の追加状態がわからず、追加後に解除できない

#### 対象ページ

アイコン合成ページ（`/icons/new`）

#### 対象箇所

「合成オプション」内の「背景を追加」セクションと、「文字の背景をつける」ボタン。

#### 詳細

現在の操作は通常のボタンとして表示されているため、押した後に文字背景が有効になっているのか、ボタンをもう一度押すとどうなるのかが画面から判断できない。

また、背景を追加した後に背景をなくすための操作が用意されていない。試しに背景を付けたユーザーが元の状態へ戻せず、設定をやり直しにくい。

文字背景は一度だけ実行する命令ではなく、「背景あり／背景なし」という二つの状態を切り替える設定なので、通常のボタンよりも状態が見えるフォームコントロールが適している。

#### 改善案

「文字の背景をつける」ボタンをチェックボックスへ変更する。

表示例は以下。

> ☐ 文字に背景をつける

チェックすると背景を追加し、チェックを外すと背景を削除する。現在の状態をチェックの有無で常に確認できるようにする。

背景色と透明度が文字背景にだけ影響する設定であれば、チェックされていない間は該当コントロールを無効表示にするか、「文字に背景をつける」の直下へまとめる。これにより、設定同士の関係も理解しやすくなる。

カスタムデザインのチェックボックスを使う場合も、見た目だけのスイッチにせず、HTML上はチェックボックスとして実装し、キーボード操作と現在状態の読み上げに対応する。

#### 関連実装

`app/views/icons/new.html.slim` の「背景を追加」部分と、`app/javascript/controllers/canvas_controller.js` の背景描画処理。チェック状態に応じて背景を追加・削除できるようにする。

---

### DR-018 透明度を変更しても元画像が透けず、背景色が白に近づく

#### 対象ページ

アイコン合成ページ（`/icons/new`）

#### 対象箇所

「合成オプション」内の透明度スライダーと、文字背景へ反映される描画結果。

#### 詳細

透明度スライダーを操作すると見た目が変化することはある。しかし、文字背景の下に元のアイコン画像が透けて見えるのではなく、背景色が白に近づくだけで、透明には見えない。

つまり、コントロール自体がまったく反応しないのではなく、反応した結果が「透明度の変更」として正しくない。黒い背景であれば、透明度を下げるほど元画像が見えることが期待されるが、実際には黒からグレー、白へ色が変化したように見える。

加えて、一度透明度を変更してからスライダーを元の値へ戻しても、表示結果が変更前の状態へ戻らないことがある。設定値とプレビューの見た目が一致せず、同じ設定値でも操作履歴によって結果が変わる可能性がある。

例えば黒い背景の透明度を50%にした場合は、半透明の黒を通して元画像が見えることが期待される。現在のように白や薄いグレーへ変化すると、ユーザーには色の明るさを調整する機能のように感じられる。

実装上は、透明度を変更する際に背景部分を消去しているが、その下に元画像を描き直していない。そのため、消去された透明領域がプレビューの白い背景を透過し、白に近づいたように見えている。また、Canvasの透明度が背景だけでなく、その後に描画する文字にも継続して適用される構造になっている。

描画結果へ変更を重ねる方式になっていることも、値を元へ戻しても最初の状態を復元できない原因になる。スライダーの現在値から完成状態を毎回作り直す必要がある。

#### 改善案

透明度を変更するたびに、Canvas全体を次の順序で最初から描き直す。

1. Canvasをクリアする
2. 元のアイコン画像を不透明で描画する
3. 指定された透明度で文字背景を描画する
4. 透明度を元へ戻す
5. 文字を不透明で描画する

透明度は文字背景のアルファ値にだけ適用し、元画像と文字には影響させない。背景を完全に透明にした場合は、背景の範囲にも元画像がそのまま見えるようにする。

操作は可逆的にし、例えば透明度を100%から50%へ変更し、再び100%へ戻した場合、変更前と同じ見た目へ完全に戻るようにする。連続してスライダーを動かした場合も、過去の描画結果を累積させず、現在値だけを反映する。

修正後は、少なくとも以下の操作順で結果が一貫することを確認する。

- 背景を追加してから透明度を変更する
- 背景色を変更してから透明度を変更する
- テキストを変更してから透明度を変更する
- 透明度を下げた後に100%へ戻す
- スライダーを連続して往復させる
- プリセットを適用した後に透明度を変更する

UI上のラベルも、対象を明確にするため「透明度」ではなく「背景の透明度」とする。現在値は `50%` のように併記する。

#### 関連実装

`app/javascript/controllers/canvas_controller.js` の `render()` と `changeGlobalOpacity()`。描画状態を部分的に上書きするのではなく、保持している元画像を含めて毎回再描画し、背景の描画時だけ `globalAlpha` を適用する。

---

### DR-019 「元画像」のプレビューは目的がわからず不要

#### 対象ページ

アイコン合成ページ（`/icons/new`）

#### 対象箇所

画像アップロード後に表示される「元画像」と「プレビュー」のうち、「元画像」と書かれた左側の表示領域。

#### 詳細

ユーザーが確認したいのは、文字や背景を合成した後の完成イメージである。アップロードした元画像はユーザー自身が選択したものなので、編集画面内であらためて常時表示する必要性が低い。

「元画像」と「プレビュー」が並ぶことで画面の幅を分け合い、重要な完成プレビューが小さくなっている。モバイルでは限られた横幅の中に2つの正方形が並ぶため、どちらも細部を確認しづらい。

また、「元画像」が比較表示なのか、編集対象なのか、クリックできるのかが明確でなく、何のために存在するのかわかりにくい。

#### 改善案

「元画像」の表示領域を削除し、完成プレビューだけを大きく表示する。

画像を変更する必要がある場合は、プレビューの近くに以下のような操作を用意する。

- 「画像を変更」ボタン
- 現在選択中のファイル名
- プレビュー上の画像変更アイコン

これにより、元画像専用の表示領域を使わずに、選択した画像を変更できることを伝えられる。

デスクトップでは大きな完成プレビューと合成オプションを2カラムで配置し、モバイルでは完成プレビューを画面幅に近いサイズで表示する。編集結果を確認しながら設定を変更できる構成にする。

#### 関連実装

`app/views/icons/new.html.slim` の「元画像」見出し、`originalImageFrame`、`originalImage` の表示部分。アップロード処理に必要な元画像データは内部的に保持しつつ、画面上の専用プレビューだけを削除する。

---

### DR-020 未ログイン時にも「アイコン一覧へ」が表示され、空の一覧へ遷移してしまう

#### 対象ページ

未ログイン状態の以下のページ。

- アイコン合成ページ（`/icons/new`）
- アイコン一覧ページ（`/icons`）

#### 対象箇所

アイコン合成ページ上部の「← アイコン一覧へ」リンクと、遷移先に表示される「保存されているアイコンはありません」という案内。

#### 詳細

未ログインのユーザーにも「← アイコン一覧へ」というリンクが表示されている。しかし、未ログイン状態ではサイト上へアイコンを保存できないため、リンク先へ移動しても「保存されているアイコンはありません」と表示されるだけになる。

存在しないコンテンツへの導線になっており、ユーザーに不要な移動をさせている。また、「アイコン一覧へ」というラベルから何らかのアイコンが見られることを期待させるため、空の画面が表示されると機能が動いていないようにも感じられる。

#### 改善案

未ログイン時は、アイコン合成ページ上部の「← アイコン一覧へ」リンクを表示しない。ログイン済みで、保存済みアイコンを確認できるユーザーにだけ表示する。

代わりに、アイコン合成ページの下部へ、ログインすると利用できる保存機能を前向きに伝える文章とログインリンクを配置する。

表示例は以下。

> 作ったアイコンを保存して、あとから確認できます  
> Googleアカウントでログインすると、合成したアイコンをサイトに保存できます。

リンクまたはボタンの文言例。

> Googleアカウントでログインする

この案内は、画像の合成とダウンロードを妨げないよう、主要な作業領域やダウンロード操作の後に置く。外部サービスの変更ガイドが続く場合は、その前に配置すると、作業完了後の次の行動として認識しやすい。

ログインを必須条件のように見せず、「ログインすると保存できる」という追加の利便性として表現する。

#### 関連実装

`app/views/icons/new.html.slim` の「← アイコン一覧へ」リンクを、ログイン状態に応じて表示する。アイコン合成ツールの下部には、未ログイン時だけ表示されるログイン案内と認証リンクを追加する。

---

### DR-021 「アイコンを変更」ボタンがdisabled状態に見える

#### 対象ページ

アイコン合成ページ（`/icons/new`）

#### 対象箇所

画像をアップロードした後、「アイコンを選択」から文言が変わって表示される「アイコンを変更」ボタン。

#### 詳細

画像アップロード後の「アイコンを変更」ボタンには、グレーの背景色を使った `is-muted` スタイルが適用される。一般的にグレーで彩度の低いボタンはdisabled状態を表すことが多いため、実際には操作可能でも押せないように見える。

「アイコンを変更する場合はこちら」という周辺テキストも薄い色へ変わるため、画像変更機能全体が無効化された印象を強めている。

#### 改善案

主要な「アイコンを選択」ボタンよりは控えめにしつつ、操作可能であることが明確なセカンダリーボタンとして表示する。

例えば、次のような表現が考えられる。

- 白い背景とインディゴまたは濃いグレーの枠線
- 十分なコントラストを持つ文字色
- 画像変更を連想できるアイコン
- ホバー、フォーカス時の明確な変化
- 通常のポインターではなく、クリック可能と伝わるカーソル

disabled状態を表すグレー、低い不透明度、薄い文字色は、実際に操作できない要素だけへ使用する。

周辺の「アイコンを変更する場合はこちら」はなくてもボタンだけで意味が伝わるため、削除して画面を簡潔にすることも検討する。

#### 関連実装

`app/javascript/controllers/ui_state_controller.js` で追加している `is-muted` クラスと、`app/assets/tailwind/application.css` の `.upload-button.is-muted` および `.instruction-text.is-muted`。

---

### DR-022 ヘッダーの「ログイン」リンクがトップページへ戻るだけで不要

#### 対象ページ

未ログイン状態で共通ヘッダーが表示されるページ。特にアイコン合成ページ（`/icons/new`）。

#### 対象箇所

ヘッダー右側の「ログイン」リンク。

#### 詳細

「ログイン」というラベルからは、クリックするとGoogleログインが開始されるか、ログイン方法を選ぶ画面へ移動することが期待される。しかし、実際にはトップページへ戻るため、ラベルと遷移先が一致していない。

アイコン合成ページで作業している途中に押した場合は、意図せずトップページへ移動することになる。ログイン操作のためにページを移動させる必要もなく、ヘッダーへ常時表示する意義が薄い。

#### 改善案

未ログイン時に表示されているヘッダーの「ログイン」リンクを削除する。

ログインへの導線は、以下のように目的とメリットを説明できる場所へ集約する。

- トップページの「ログインして作る」という選択肢
- アイコン合成ページ下部の「作ったアイコンを保存できます」という案内

これにより、ユーザーはログインによって何が便利になるのか理解したうえで操作できる。合成画面のヘッダーは、ロゴなど必要最小限の要素だけにする。

#### 関連実装

`app/views/layouts/_header.html.slim` の未ログイン時に表示される「ログイン」リンク。ログイン状態によるヘッダー表示を見直し、ログイン導線はウェルカムページとアイコン合成ページ下部へ配置する。

---

### DR-023 テキストを変更すると前の文字が消えずに重なる

#### 対象ページ

アイコン合成ページ（`/icons/new`）

#### 対象箇所

「合成オプション」内の「ラジオ参加」「離席中」「画面見れません」などのテキスト追加ボタンと、完成プレビュー。

#### 詳細

一つのテキストを追加した後に別のテキストを選択すると、前の文字が消えず、同じ位置へ新しい文字が重なって描画される。そのため文字が読めなくなり、選択を変更して結果を比較することもできない。

現在のUIは一つのテキストを選択する構成に見えるため、新しいテキストを選んだ場合は、前の文字を置き換える動作が期待される。複数の文字を追加できる機能としても、位置や削除を管理するUIがないため、現在の重なりは意図的な複数追加ではなく描画上の不具合に見える。

#### 改善案

テキストを選択または入力するたびに、現在のCanvasへ直接描き足さず、保持している最新設定からプレビュー全体を再描画する。

描画順序は以下を基本とする。

1. Canvasをクリアする
2. 元画像を描画する
3. 有効な場合は文字背景を描画する
4. 現在選択されている一つのテキストを描画する

別の定型文を選んだ場合は、保持しているテキスト値を更新し、新しい値だけを描画する。任意テキスト入力を追加する場合も同じ仕組みに統一する。

選択中の定型文ボタンには、色や枠線などで選択状態を表示し、現在どのテキストが適用されているか画面上でも確認できるようにする。

将来的に複数テキストを配置できるようにする場合は、各テキストの選択、位置変更、削除を管理できる別のUIが必要になる。現在の単一選択UIでは、置き換え動作とするのが自然である。

#### 関連実装

`app/javascript/controllers/canvas_controller.js` の `drawText()` と `render()`。Canvasへ過去の描画を残したまま追加せず、元画像と最新の `renderPlan` から毎回プレビューを再構築する。

---

### DR-024 ゴールである「画像をダウンロード」がコンテンツに埋もれている

#### 対象ページ

アイコン合成ページ（`/icons/new`）

#### 対象箇所

合成オプションの下にある「画像をダウンロード」リンクと、その周辺の補足文・区切り線。

#### 詳細

このページにおけるユーザーの最終的なゴールは、作成した画像をダウンロードすることである。しかし、現在のダウンロード操作は編集領域の下部にある小さめのボタンとして表示され、補足文や後続の変更ガイドに埋もれている。

初期状態ではdisabled表示になっているため存在感がさらに弱く、画像を作成した後に何をすれば完了なのかを、画面全体から見つけにくい。ページ内では「アイコンを選択」ボタンの方が視覚的に強く、開始操作と完了操作の重要度が逆転して見える。

#### 改善案

ダウンロード操作を、編集フローの独立した最終ステップとして明確に表示する。

例えば、以下のように構成する。

1. STEP 1：画像を選ぶ
2. STEP 2：文字や背景を調整する
3. STEP 3：完成した画像をダウンロードする

ダウンロードCTAは、編集カードの最下部に十分な余白を持つ専用領域を設け、後続のサービス変更ガイドとは明確に分離する。

ボタンは主要CTAとして、次のように表現する。

- 文言を「完成した画像をダウンロード」など、結果が伝わる表現にする
- インディゴなどの主要色を使う
- 本文の基準である16px以上の文字を使う
- 十分な横幅と高さを持たせる
- ダウンロードアイコンを添える
- 有効になった際の変化を明確にする

画像がまだ作成されていない状態では、単に薄くするだけでなく、「文字を追加するとダウンロードできます」のように、利用可能になる条件を近くへ表示する。

モバイルでは、編集操作中にCTAを見失わないよう、プレビューと設定の直後に大きく表示する。常時画面下部へ固定する場合は、本文やフォームを覆わないこと、無効状態のボタンが常に場所を占有しないことを確認する。

#### 関連実装

`app/views/icons/new.html.slim` のダウンロードリンクと、その周辺の補足文を含む領域。主要CTA用の独立したセクションへ再構成し、`app/assets/tailwind/application.css` で有効・無効状態の視覚的な差を調整する。

---

### DR-025 利用規約・プライバシーポリシーのタイトルルールが他ページと異なり、本文との間隔も狭い

#### 対象ページ

- 利用規約ページ（`/terms`）
- プライバシーポリシーページ（`/privacy`）

#### 対象箇所

ページ冒頭の「利用規約」「プライバシーポリシー」というタイトルと、それぞれの直後に始まる本文。

#### 詳細

利用規約ページとプライバシーポリシーページでは、タイトルが本文コンテナ内の中央揃え見出しとして表示されている。一方、アイコン合成ページでは、ページタイトルが独立したページヘッダー内へ左揃えで配置されているため、ページタイトルの位置、揃え方、周囲の余白に一貫性がない。

また、各ページタイトルと最初の本文との下方向の余白が小さく、タイトルと本文が詰まって見える。長文を読むページでは、ページタイトルと本文の開始位置を明確に分け、読者が文書の入口を認識しやすくする必要がある。

#### 改善案

サイト内でページタイトルの共通ルールを定め、利用規約ページとプライバシーポリシーページの両方へ適用する。

少なくとも以下を統一する。

- タイトルを表示する領域
- 左揃えまたは中央揃えの方針
- タイトルの文字サイズと太さ
- ページ左右の余白
- タイトル領域の上下余白
- 本文が始まるまでの間隔

一般的な下層ページとして見せる場合は、アイコン合成ページと同様に、タイトルを独立したページヘッダーへ左揃えで配置する。その下に本文コンテナを置き、タイトル領域と本文の間に十分な余白を設ける。

現在の中央揃えを維持する場合でも、タイトル下の余白を増やし、本文との境界が明確に見えるようにする。現在の `mb-3` よりも広い間隔を基準に、デスクトップとモバイルの両方で調整する。

利用規約とプライバシーポリシーは同じ文書ページ用レイアウトを共有し、タイトルルールが両ページでずれないようにする。

#### 関連実装

`app/views/home/terms.html.slim` の冒頭にある `header.mb-3` とタイトル部分。あわせて、同じ構造を持つ `app/views/home/privacy.html.slim` も共通のページタイトルコンポーネントまたは同じスタイルへ揃える。

---

### DR-026 トップページの見出しと説明文から、サービスの内容が伝わりにくい

#### 対象ページ

ウェルカムページ（`/welcome`）

#### 対象箇所

ファーストビューの見出しと、その直下にある説明文。

現在の文言は以下。

> オンラインイベントで  
> 「ラジオ参加」を発言せずに伝える

> 自分のアイコン画像に「今日はラジオ参加」などのテキストを1クリックで合成し、ダウンロードすることができます。  
> 主要なサービスのアイコン変更リンク、ガイドが揃っているのですぐにアイコンを変更することができます。

#### 詳細

見出しは利用場面を表しているが、このサイトで何を作れるのかを直接説明していない。「ラジオ参加」という言葉を知らないユーザーには意味が伝わりにくく、知っているユーザーにも、アイコン画像へ文字を追加するサービスであることが見出しだけではわからない。

説明文では、画像合成、ダウンロード、外部サービスへの変更ガイドという複数の機能を続けて説明している。そのため、最も重要な「アイコンに文字を入れた画像を作れる」という価値が埋もれている。

また、「発言せずに伝える」と「アイコンを変更する」の関係を頭の中で補う必要があり、サービスを初めて見たユーザーにとって理解の負担が大きい。

#### 改善案

見出しでは、ユーザーが作れるものを短く直接伝える。説明文では、利用場面と操作の簡単さを補足する。

文言案1：機能を最も直接的に伝える場合。

> **アイコンに文字を入れて、参加状況を伝えよう**  
> いつものアイコンに「ラジオ参加」「離席中」などの文字を追加して、すぐにダウンロードできます。

文言案2：オンラインイベントでの用途を強調する場合。

> **オンラインイベント用の文字入りアイコンをかんたん作成**  
> アイコンに「ラジオ参加」「離席中」などの文字を追加すれば、声を出さなくても今の状況を伝えられます。

文言案3：より簡潔にする場合。

> **今の状況が伝わる、文字入りアイコンを作ろう**  
> 画像を選んで文字を追加するだけ。登録なしで作成・ダウンロードできます。

外部サービスの変更ガイドは、このサービスの中心的な価値ではなく作成後の補助機能なので、ファーストビューの説明文から外す。完成イメージのビジュアルと組み合わせ、文章を読まなくても「画像へ文字を追加するサービス」だと理解できるようにする。

#### 関連実装

`app/views/home/welcome.html.slim` のメイン見出しと、その直下にある2文のサービス説明。外部サービスの変更ガイドに関する説明は、該当セクションの近くへ移動する。

---

### DR-027 ロゴの青とUIの青が揃っていない

#### 対象ページ

ウェルカムページ、アイコン合成ページを含むサイト全体。

#### 対象箇所

- ロゴに使われている青
- ページタイトル
- 主要ボタン
- テキストリンク
- 見出しの区切り線
- ホバーやフォーカス時の色
- 薄い青の背景や枠線

#### 詳細

ロゴに使われている青と、UIの主要色として使われているインディゴ系の青が微妙に異なる。どちらもブランドを表す色として目立つため、近いが一致しない色が同じ画面にあると、意図した使い分けではなく色のずれに見える。

UI内でも複数段階のインディゴや青が使われており、色の種類が増えている。サービス全体はシンプルな構成なので、似た青を多数使うよりも、一つの基準色と必要最小限の濃淡へ絞った方が一貫した印象になる。

#### 改善案

ロゴとUIで共通して使用するブランドカラーを一つ決め、主要な青をその色へ統一する。

使用する色は、例えば以下の最小限の体系にする。

- **ブランドカラー**：ロゴ、主要CTA、重要なリンク、選択状態
- **濃いブランドカラー**：ホバー、押下状態、十分なコントラストが必要な文字
- **薄いブランドカラー**：選択項目や案内の背景
- **ニュートラルカラー**：本文、補助情報、枠線、disabled状態

ロゴの現在色を基準にUIを合わせるか、UIで使う青を正式なブランドカラーとしてロゴを調整するかを決める。どちらを選ぶ場合も、色コードをデザイントークンとして一元管理する。

青を装飾目的で多用せず、主要な操作、現在選択されている状態、重要なリンクに限定する。通常の見出しや区切り線は黒・グレーを基本にすると、主要CTAの青がより明確になる。

色を統一した後は、白背景上の文字やボタンが十分なコントラストを持つこと、ホバー・フォーカス・disabled状態を色だけに依存せず区別できることも確認する。

#### 関連実装

`app/assets/tailwind/application.css` と各Slimテンプレートで使われている `indigo-*`、`text-primary`、ボタン・リンクの色指定、およびロゴ画像の配色。ブランドカラーをCSS変数またはTailwindの共通テーマとして定義し、個別の色指定を置き換える。

---

## 良かった点

### GD-001 色数を抑えたシンプルなデザインになっている

#### 対象ページ

ウェルカムページ、アイコン合成ページを含むサイト全体。

#### 対象箇所

見出し、主要ボタン、リンク、カード、背景色。

#### 詳細

白とグレーを中心に色数が抑えられており、全体として清潔感がある。主要な操作に青系の色を使う方針も理解しやすい。ロゴとUIで青の色味を統一すれば、さらに一貫した印象になる（`DR-027`）。

---

### GD-002 ウェルカムページの主要CTAが視覚的に目立っている

#### 対象ページ

ウェルカムページ（`/welcome`）

#### 対象箇所

「アイコンに文字を合成する」ボタンと「登録なしですぐに使えます！」の案内。

#### 詳細

サービスをすぐに試すためのボタンが最も強く表示されている点はよい。「登録なしで利用できる」という情報も、利用開始前の不安を減らしている。2つのCTAの意味と違いを整理すれば、より直感的な導線になる（`DR-010`）。

---

### GD-003 モバイルで横方向のレイアウト崩れがない

#### 対象ページ

アイコン合成ページ（`/icons/new`）

#### 対象箇所

ページ全体のモバイルレイアウト。

#### 詳細

390 × 844pxの表示では横スクロールが発生せず、画像フレーム、合成オプション、サービスガイドが画面幅に収まっていた。デスクトップの3カラム構成からモバイルの縦方向構成への切り替えも機能している。

---

### GD-004 基本的なアクセシビリティ構造が整っている

#### 対象ページ

アイコン合成ページ（`/icons/new`）

#### 対象箇所

見出し、アイコン合成ツール、テキスト追加ボタン、カラー設定。

#### 詳細

見出し構造、`fieldset` と `legend`、操作グループのARIAラベル、画像の代替テキストなどが設定されている。確認範囲ではブラウザコンソールにも警告やエラーはなかった。
